import { DefaultTheme } from 'styled-components';
import { Size } from '../../constants/size';
interface Props {
    theme: DefaultTheme;
    $size: Size;
}
export declare const getSize: ({ theme, $size }: Props) => string;
export default getSize;
//# sourceMappingURL=getSize.d.ts.map