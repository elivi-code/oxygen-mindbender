import { DefaultTheme } from 'styled-components';
import { Status } from '../../constants/status';
interface Props {
    theme: DefaultTheme;
    $status: Status;
}
export declare const getColor: ({ theme, $status }: Props) => string;
export default getColor;
//# sourceMappingURL=getColor.d.ts.map