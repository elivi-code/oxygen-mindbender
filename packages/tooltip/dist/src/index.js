import Tooltip from './components/Tooltip';
import { orientation, showOn } from './types';
export default Tooltip;
export { orientation, orientation as directions, // deprecated
showOn, showOn as showOnTypes, // deprecated
 };
