import type { Theme, ToastSize } from '../../types';
type getWidthProps = {
    size: ToastSize;
    theme: Theme;
};
declare const getWidth: ({ theme, size }: getWidthProps) => string;
export default getWidth;
//# sourceMappingURL=getWidth.d.ts.map