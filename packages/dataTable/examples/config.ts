const blurOnTab = {
  control: {
    type: 'boolean',
  },
  description:
    'If true, pressing Tab will move focus out of the table, otherwise it will cycle through tabbable elements within the table.',
};

export const argTypes = {
  blurOnTab,
};
