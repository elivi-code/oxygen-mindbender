import styled from 'styled-components';
import themes from '@8x8/oxygen-theme';

const { novo } = themes;

const HeaderSubtitle = styled.div`
  margin-top: ${novo.spacing03};
`;

export default HeaderSubtitle;
