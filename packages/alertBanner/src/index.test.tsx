import AlertBannerUsage from '.';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import type { AlertBannerProps } from './types';

describe('AlertBannerUsage exports', () => {
  it('should export Alert', () => {
    expect(AlertBannerUsage).toBeDefined();
  });
});
