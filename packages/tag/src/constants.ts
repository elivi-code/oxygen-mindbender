const variants = {
  light: 'light',
  dark: 'dark',
  blue: 'blue',
  yellow: 'yellow',
  red: 'red',
} as const;

const HEIGHT = 1.5;
const HEIGHT_REM = `${HEIGHT}rem`;

export { variants, HEIGHT, HEIGHT_REM };
